Legacy FastAPI experiment archived here.

This code is preserved for reference only. The production path for this repository is the Node/Express backend in `server.js` with the static frontend in `index.html`, `css/style.css`, and `js/main.js`.
